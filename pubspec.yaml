name: pixabay
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_bloc: 9.1.0
  bloc: 9.0.0
  dio: ^5.8.0+1
  equatable: ^2.0.7
  cached_network_image: ^3.4.1

flutter:
  uses-material-design: true
